const config = require("../../local.settings.json")
import {SignupRequest} from '../model/Requests'

export class MongoRepository{
  constructor(url:string){

  }

  signUp(signupRequest: SignupRequest) : void {

  }
}